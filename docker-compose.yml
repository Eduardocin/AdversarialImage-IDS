services:
  legacy:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: adversarialimage-ids:legacy
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      PYTHONPATH: /opt/caffe/python:/workspace/src/original/DeepDetector
    stdin_open: true
    tty: true
